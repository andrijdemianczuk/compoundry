services:
  agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file: ../.env
    ports:
      - "8080:8080"
    volumes:
      - ../data:/app/data
      - ../notes:/app/notes

volumes: {}
